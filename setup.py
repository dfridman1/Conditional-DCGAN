#!/usr/bin/env python
from setuptools import setup, find_packages

setup(
    name='cgan',
    version='0.0.1',
    description='Conditional GAN',
    packages=find_packages(include=["cgan"]),
    install_requires=[
    ],
)
